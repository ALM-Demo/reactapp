FROM node:8
ADD yarn.lock /yarn.lock
ADD package.json /package.json
ENV NODE_PATH=/node_modules
ENV PATH=$PATH:/node_modules/.bin
RUN yarn

RUN chown -R 1100:node /tmp
WORKDIR /tmp
USER 1100
ADD  --chown=1100:node . /tmp
RUN chmod -R u+x /tmp/*.sh
EXPOSE 3000
EXPOSE 35729
ENTRYPOINT ["/bin/bash", "/tmp/run.sh"]
CMD ["start"]
